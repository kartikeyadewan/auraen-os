# Configuration Directory

This directory contains default system configuration files for Auraen OS.

These configs are applied during ISO build and include settings for:

- Shell (ZSH + Oh My ZSH)
- Desktop environment (GNOME)
- Editor defaults
- System-wide environment variables
